# T2.6 / T2.8 / T2.7 / Telegram — árbol, sort, colores, avisos

## Hecho

* Catálogo conmuta lista ↔ árbol. Hojas = username; carpetas desde `field1`.
* Sort visual A↔Z en la tabla del lote y en diálogos de URLs (no en Lotes).
* Paleta de colores de catálogo en Configuración.
* `notify_service` + `send_message_to`; fin de lote y errores del bot
  marcados. UI: destino, plantilla, checkboxes de `bot_errors`, prueba.

## Fuera de alcance

* Rediseño del diálogo Lotes / ejecuciones.
